# Use when you want to place something immediately when webapp starts
# Run python manage.py runserver   ->   to run the webapp
# Run python manage.py migrate     ->   to run format the db.sqlite3 file, populating it with default tables 